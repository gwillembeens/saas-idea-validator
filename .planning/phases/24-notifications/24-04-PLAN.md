---
plan: 24-04
title: "Phase 24 — NotificationsPage & Route"
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - client/src/pages/NotificationsPage.jsx
  - client/src/App.jsx
requirements: [NOTIF-01, NOTIF-02]
autonomous: true
---

## Summary

Create `/notifications` page showing full grouped notification history with cards. Fetch on mount, render grouped notifications with card decoration, empty state, and mark-all-read button. Register route in App.jsx.

---

## Task 24-04-00: Create NotificationsPage

**Complexity:** Medium  
**Estimated Time:** 35 minutes

<read_first>
- `client/src/pages/HistoryPage.jsx` (page pattern reference)
- `client/src/pages/LeaderboardPage.jsx` (card grid pattern)
- `client/src/components/ui/Card.jsx` (Card component)
</read_first>

<action>
Create `/Users/gijsbeens/Projects/saas-idea-validator/saas-idea-validator/client/src/pages/NotificationsPage.jsx`:

```javascript
import { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { AppShell } from '../components/layout/AppShell'
import { Card } from '../components/ui/Card'
import { Button } from '../components/ui/Button'
import { useNotifications } from '../hooks/useNotifications'

export function NotificationsPage() {
  const navigate = useNavigate()
  const { notifications, fetchNotifications, markAllRead } = useNotifications()
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    async function load() {
      setIsLoading(true)
      try {
        await fetchNotifications()
      } finally {
        setIsLoading(false)
      }
    }
    load()
  }, [fetchNotifications])

  const handleNotificationClick = (notification) => {
    const resultId = notification.result_id
    if (notification.event_type === 'comment') {
      navigate(`/history/${resultId}`, { state: { openComments: true } })
    } else {
      navigate(`/history/${resultId}`)
    }
  }

  return (
    <AppShell>
      <div className="max-w-2xl mx-auto px-4 py-20">
        {/* Header */}
        <div className="flex items-center justify-between mb-8">
          <h1 className="font-heading text-5xl text-pencil">Notifications</h1>
          {notifications.length > 0 && (
            <Button variant="secondary" onClick={markAllRead}>
              Mark all as read
            </Button>
          )}
        </div>

        {/* Empty state */}
        {!isLoading && notifications.length === 0 && (
          <Card decoration="tack" className="p-8 text-center">
            <p className="font-body text-lg text-pencil mb-2">
              No notifications yet
            </p>
            <p className="font-body text-sm text-pencil opacity-60">
              Share a validation to start getting likes and comments from the community
            </p>
          </Card>
        )}

        {/* Loading state */}
        {isLoading && (
          <div className="space-y-4">
            {[1, 2, 3].map(i => (
              <Card key={i} decoration="none" className="p-4 animate-pulse">
                <div className="h-4 bg-muted rounded w-1/2"></div>
                <div className="h-3 bg-muted rounded w-1/3 mt-2"></div>
              </Card>
            ))}
          </div>
        )}

        {/* Notification list */}
        {!isLoading && notifications.length > 0 && (
          <div className="space-y-4">
            {notifications.map((notif, idx) => (
              <Card
                key={`${notif.event_type}-${notif.result_id}-${idx}`}
                decoration={notif.is_unread ? 'tack' : 'none'}
                className={`p-4 cursor-pointer transition-transform hover:rotate-1 ${
                  notif.is_unread ? 'bg-postit/5' : ''
                }`}
                onClick={() => handleNotificationClick(notif)}
              >
                <div className="flex items-start justify-between">
                  <div className="flex-1 min-w-0">
                    <p className="font-body text-sm text-pencil">
                      <span className="font-bold">
                        {notif.most_recent_actor_username || 'Someone'}
                      </span>
                      {' '}
                      {notif.event_type === 'like' ? 'liked' : 'commented on'}
                      {' '}
                      <span className="italic">"{notif.validation_title}"</span>
                    </p>
                    <p className="font-body text-xs text-pencil opacity-60 mt-2">
                      {notif.actor_count > 1 && `${notif.actor_count} people · `}
                      {new Date(notif.most_recent_at).toLocaleDateString(undefined, {
                        month: 'short',
                        day: 'numeric',
                        year: new Date(notif.most_recent_at).getFullYear() !== new Date().getFullYear() ? 'numeric' : undefined,
                        hour: '2-digit',
                        minute: '2-digit',
                      })}
                    </p>
                  </div>
                  {notif.is_unread && (
                    <div className="ml-4 flex-shrink-0">
                      <span
                        className="inline-block w-3 h-3 bg-accent rounded-full"
                        style={{ borderRadius: '50%' }}
                      ></span>
                    </div>
                  )}
                </div>
              </Card>
            ))}
          </div>
        )}
      </div>
    </AppShell>
  )
}
```

This page:
- Fetches full notifications list on mount
- Shows empty state with engagement copy
- Shows skeleton loaders while loading
- Renders notifications as Cards with click-to-navigate
- Comment notifications navigate with openComments state
- Like notifications navigate without state
- Unread notifications have tack decoration + subtle yellow background + blue dot
- Mark all as read button in header
- Hover effect on cards
</action>

<acceptance_criteria>
- [ ] File `client/src/pages/NotificationsPage.jsx` exists
- [ ] Page imports: useNavigate, useNotifications, AppShell, Card, Button
- [ ] On mount, calls `fetchNotifications()` via useEffect
- [ ] Shows loading skeleton with 3 cards while isLoading=true
- [ ] Shows empty state card when notifications.length=0: "No notifications yet" + "Share a validation..."
- [ ] Each notification renders in a Card with:
  - Actor username OR "Someone"
  - Event verb (liked/commented on)
  - Validation title in italics
  - Actor count (if > 1) and timestamp
  - Unread indicator (blue dot) if is_unread=true
  - Card decoration: 'tack' if unread, 'none' if read
- [ ] Clicking card navigates to `/history/:id` with comment state if event_type='comment'
- [ ] Clicking card navigates to `/history/:id` without state if event_type='like'
- [ ] Mark all as read button calls `markAllRead()`
- [ ] Mark all as read button hidden when notifications.length=0

Verify with:
```bash
node -c client/src/pages/NotificationsPage.jsx
```
</acceptance_criteria>

---

## Task 24-04-01: Register NotificationsPage Route in App.jsx

**Complexity:** Low  
**Estimated Time:** 10 minutes

<read_first>
- `/Users/gijsbeens/Projects/saas-idea-validator/saas-idea-validator/client/src/App.jsx`
</read_first>

<action>
In `client/src/App.jsx`:

1. Add import at the top with other page imports:
```javascript
import { NotificationsPage } from './pages/NotificationsPage'
```

2. Add route in the `<Routes>` section (after line 68, before or after profile route):
```javascript
      <Route path="/notifications" element={<NotificationsPage />} />
```
</action>

<acceptance_criteria>
- [ ] Import statement added: `import { NotificationsPage } from './pages/NotificationsPage'`
- [ ] Route registered: `<Route path="/notifications" element={<NotificationsPage />} />`
- [ ] Route is inside the `<Routes>` block
- [ ] App starts without errors: `npm run dev`
- [ ] Navigating to `/notifications` renders NotificationsPage
- [ ] Navigation from NotificationDropdown footer link works: `navigate('/notifications')`

Verify with:
```bash
npm run dev  # Check no errors
# Manually click "View all notifications →" in dropdown or visit http://localhost:5173/notifications
```
</acceptance_criteria>

---

## Success Criteria (24-04)

- [ ] `/notifications` route renders NotificationsPage
- [ ] Full notification history loads on page mount
- [ ] Notifications grouped by (event_type, result_id)
- [ ] Click notification navigates to result
- [ ] Comment notifications auto-open CommentModal
- [ ] Empty state shown when no notifications
- [ ] Mark all as read button works
- [ ] Unread indicators visible (tack decoration, blue dot)

---