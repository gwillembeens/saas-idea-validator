# Plan 23-04: Frontend Wave 3 — Profile ValidationsGrid + Leaderboard Sort

**Phase:** 23 — Social Interactions
**Plan:** 04 of 04
**Status:** Ready

## Overview
Add the paginated validations grid to the profile page, wire `total_likes_received` into the stat bar, update the heatmap tooltip to say "actions", build the "Most Liked" sort toggle on the leaderboard, and connect `CommentModal` to all card surfaces. This is the final plan in Phase 23 — all backend (23-01) and shared UI (23-02, 23-03) must be complete first.

## Prerequisites
- Plan 23-01 complete: profile route returns `like_count`, `comment_count`, `total_likes_received`; leaderboard supports `?sort=most_liked`
- Plan 23-02 complete: `LikeButton`, `CommentCount`, `ProfileValidationCard` extended
- Plan 23-03 complete: `CommentModal` component available

## Tasks

### Task 23-04-00: Create `ValidationsGrid` Component
**Goal:** Paginated 2-column grid of profile validation cards

**Files:**
- `client/src/components/profile/ValidationsGrid.jsx` — New file
  - Props: `validations: []` (full list from profile response)
  - Internal state: `visibleCount: number` (starts at 6)
  - Renders:
    - Section header: `font-heading text-2xl text-pencil` "Validations"
    - 2-col grid: `grid grid-cols-1 md:grid-cols-2 gap-4`
    - `validations.slice(0, visibleCount)` mapped to `<ProfileValidationCard>` (with `onCommentClick`)
    - `CommentModal` controlled by `openCommentResultId: number|null` state
    - "Load more" `<Button variant="secondary">` when `visibleCount < validations.length`:
      - On click: `setVisibleCount(v => v + 6)`
      - Label: `"Load more (N remaining)"` where N = `validations.length - visibleCount`
  - Empty state (no validations): `font-body text-pencil opacity-50 text-sm` "No public validations yet."

### Task 23-04-01: Update `ProfilePage` — ValidationsGrid + total_likes_received
**Goal:** Insert ValidationsGrid between RevisionChains and AnalyticsSection; add likes stat

**Files:**
- `client/src/pages/ProfilePage.jsx` — Modify:
  - Import `ValidationsGrid`
  - Insert `<ValidationsGrid validations={profile.validations || []} />` between `<RevisionChains>` and `<AnalyticsSection>`
  - In the stat bar (`profile.stats.total_public > 0` block), add `total_likes_received` if not null:
    ```jsx
    {profile.stats.total_likes_received != null && profile.stats.total_likes_received > 0 && (
      <>
        <span className="text-pencil opacity-20 font-body text-lg">·</span>
        <StatItem label="likes received" value={profile.stats.total_likes_received} />
      </>
    )}
    ```
  - Place after `personal_best` stat and before streak stats

### Task 23-04-02: Update Heatmap Tooltip in `AnalyticsSection`
**Goal:** Change tooltip from "N validations" to "N actions"

**Files:**
- `client/src/components/profile/AnalyticsSection.jsx` — Modify:
  - Locate the heatmap tooltip text (currently says something like `"${count} validation${count !== 1 ? 's' : ''}"`)
  - Change to `"${count} action${count !== 1 ? 's' : ''}"`
  - If the backend now returns `heatmap_label: 'actions'` in analytics, use that field; otherwise hardcode `'actions'`

### Task 23-04-03: Create `LeaderboardSort` Component
**Goal:** Tab-pill sort toggle: "Top Score" / "Most Liked"

**Files:**
- `client/src/components/leaderboard/LeaderboardSort.jsx` — New file
  - Props: `sort: 'score' | 'most_liked'`, `onSortChange: (sort) => void`
  - Renders two tab pills side by side:
    - Active pill: `bg-pencil text-paper font-body text-sm px-4 py-1.5 shadow-hardSm`
    - Inactive pill: `bg-paper text-pencil border-2 border-pencil font-body text-sm px-4 py-1.5`
    - Both with `style={{ borderRadius: '15px 225px 15px 255px / 225px 15px 255px 15px' }}`
  - Labels: "Top Score" (sort=`'score'`) and "Most Liked" (sort=`'most_liked'`)

### Task 23-04-04: Wire Sort into `LeaderboardPage`
**Goal:** Add sort state, pass `?sort=` to API, reset pagination on sort change

**Files:**
- `client/src/pages/LeaderboardPage.jsx` — Modify:
  - Add `sort` state: `useState('score')`
  - Add `LeaderboardSort` component between CTA banner and ChallengeSection (or above filter pills)
  - Pass `sort` to `useLeaderboard` hook
  - In `useLeaderboard` (or its call site), add `sort` to the API query string: `?niche=...&page=...&sort=...`
  - On sort change: reset page to 1 and clear existing entries before refetching

**Files:**
- `client/src/hooks/useLeaderboard.js` — Modify:
  - Accept `sort` parameter (default `'score'`)
  - Include `sort` in the fetch URL: `/api/leaderboard?page=${page}&niche=${niche}&sort=${sort}`
  - When `sort` changes: reset entries to `[]` and page to 1 (same pattern as niche change)

### Task 23-04-05: Wire `CommentModal` into `LeaderboardCard`
**Goal:** Clicking comment count on a leaderboard card opens CommentModal

**Files:**
- `client/src/components/leaderboard/LeaderboardCard.jsx` — Modify:
  - Add `openCommentModal: boolean` state (or lift up to LeaderboardPage — see note below)
  - Pass `onCommentClick={() => setOpenCommentModal(true)}` to `<CommentCount>`
  - Render `{openCommentModal && <CommentModal resultId={entry.id} onClose={() => setOpenCommentModal(false)} />}` at component root
  - Note: keeping modal state per-card is simplest (one modal per card, rendered in place); acceptable for this use case

### Task 23-04-06: Wire `CommentModal` into `ProfileValidationCard`
**Goal:** Clicking comment count on a profile card opens CommentModal

**Files:**
- `client/src/components/profile/ProfileValidationCard.jsx` — Modify (continuation of 23-02-03):
  - Add `openCommentModal: boolean` state
  - Wire `onCommentClick={() => setOpenCommentModal(true)}` to the `CommentCount` `onClick`
  - Render `{openCommentModal && <CommentModal resultId={validation.id} onClose={() => setOpenCommentModal(false)} />}` at component root

### Task 23-04-07: Wire `CommentModal` into `ValidationsGrid`
**Goal:** Centralise modal state in grid to avoid multiple modal renders

**Files:**
- `client/src/components/profile/ValidationsGrid.jsx` — Modify (continuation of 23-04-00):
  - `openCommentResultId: number|null` state at grid level
  - Pass `onCommentClick={(id) => setOpenCommentResultId(id)}` to each `ProfileValidationCard`
  - Replace per-card modal (23-04-06) with single grid-level `CommentModal`:
    ```jsx
    {openCommentResultId != null && (
      <CommentModal resultId={openCommentResultId} onClose={() => setOpenCommentResultId(null)} />
    )}
    ```
  - Update `ProfileValidationCard` `onCommentClick` prop signature to `() => void` (ValidationsGrid passes the bound ID)

### Task 23-04-08: End-to-End Data Flow Verification
**Goal:** Confirm data flows correctly from DB → API → UI for all social features

**Checklist (no code changes — verification only):**
- [ ] Profile page stat bar shows `total_likes_received` (comes from `profile.stats.total_likes_received`)
- [ ] Profile `ValidationsGrid` receives `like_count` and `comment_count` per validation (from `profile.validations[]`)
- [ ] Leaderboard entries include `like_count` and `comment_count` (from `/api/leaderboard` response)
- [ ] Toggling like on ResultPage updates count (confirmed via useLike optimistic + server sync)
- [ ] Heatmap tooltip on ProfilePage says "N actions" not "N validations"
- [ ] Leaderboard `?sort=most_liked` returns different ordering than `?sort=score`
- [ ] CommentModal opens from LeaderboardCard, ProfileValidationCard, and ValidationsGrid correctly

## Verification
- [ ] `ValidationsGrid` renders 6 cards initially; "Load more" reveals next 6
- [ ] "Load more" button label shows correct remaining count
- [ ] Empty profile validations shows "No public validations yet." message
- [ ] `total_likes_received` appears in stat bar when > 0
- [ ] Heatmap tooltip says "N actions" (not "N validations")
- [ ] Leaderboard sort pills render; clicking "Most Liked" changes results order
- [ ] Sort change resets to page 1 (no duplicate entries from mixed sorts)
- [ ] CommentModal opens from LeaderboardCard comment count click
- [ ] CommentModal opens from profile ValidationsGrid comment count click
- [ ] Only one CommentModal open at a time (grid-level state prevents multiple modals)

## Test Coverage
- Unit tests: `ValidationsGrid` — shows 6 initially, load-more shows +6
- Unit tests: `LeaderboardSort` — active pill changes on click
- Component tests: `ValidationsGrid` empty state
- Component tests: `ProfilePage` renders ValidationsGrid and total_likes_received
- Component tests: `LeaderboardPage` passes sort to useLeaderboard; resets on sort change
- Integration tests: clicking CommentCount on LeaderboardCard opens CommentModal with correct resultId
- Integration tests: clicking CommentCount in ValidationsGrid opens single CommentModal
