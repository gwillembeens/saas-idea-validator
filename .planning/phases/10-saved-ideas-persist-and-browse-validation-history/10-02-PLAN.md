---
plan: 10-02
phase: 10
title: "React Router Integration & Auto-save Wiring"
wave: 2
depends_on: [10-01]
status: pending
files_modified:
  - client/package.json
  - client/src/main.jsx
  - client/src/App.jsx
  - client/src/pages/HomePage.jsx
  - client/src/pages/HistoryPage.jsx
  - client/src/pages/ResultPage.jsx
  - client/src/hooks/useValidate.js
  - client/src/store/slices/validatorSlice.js
---

# Plan 10-02: React Router v6 & Auto-save Integration

## Goal

Install and configure React Router v6, reorganize App.jsx as layout wrapper, create page components for home/history/result views, wire auto-save hook to fire after validation completion, and add History nav link.

## Requirements Met

- D-01/D-02: Auto-save triggers automatically when status === 'done'
- Frontend routes: /, /history, /history/:id
- History nav link auth-gated (only shown when user is authenticated)
- Re-validate flow: load idea_text into Redux, navigate to /

## Tasks

<task id="10-02-01" title="Install react-router-dom v6">
  <read_first>
  - client/package.json — existing dependencies
  </read_first>

  <action>
  In client/ directory:
  npm install react-router-dom@6

  Reasoning: Standard React Router v6 for client-side routing with support for dynamic params like /history/:id.
  </action>

  <acceptance_criteria>
  - [ ] client/package.json includes "react-router-dom": "^6" in dependencies
  - [ ] npm install completes without errors
  </acceptance_criteria>
</task>

<task id="10-02-02" title="Wrap App with BrowserRouter in main.jsx">
  <read_first>
  - client/src/main.jsx — current entry point
  </read_first>

  <action>
  Update client/src/main.jsx:

  import { StrictMode } from 'react'
  import { createRoot } from 'react-dom/client'
  import { Provider } from 'react-redux'
  import { BrowserRouter } from 'react-router-dom'
  import { store } from './store/index.js'
  import './index.css'
  import App from './App.jsx'

  createRoot(document.getElementById('root')).render(
    <StrictMode>
      <Provider store={store}>
        <BrowserRouter>
          <App />
        </BrowserRouter>
      </Provider>
    </StrictMode>,
  )

  Reasoning: BrowserRouter enables client-side routing. Wraps Redux Provider so routes have access to store.
  </action>

  <acceptance_criteria>
  - [ ] client/src/main.jsx imports BrowserRouter from 'react-router-dom'
  - [ ] App is wrapped inside BrowserRouter
  - [ ] BrowserRouter is inside Provider
  </acceptance_criteria>
</task>

<task id="10-02-03" title="Create HomePage.jsx with input and results">
  <read_first>
  - client/src/App.jsx — current structure and components
  - client/src/components/validator/IdeaInput.jsx — reuse
  - client/src/components/validator/ResultsPanel.jsx — reuse
  - client/src/components/layout/AppShell.jsx — reuse for background
  - client/src/components/decorative/Arrow.jsx — reuse
  </read_first>

  <action>
  Create client/src/pages/HomePage.jsx with:

  import { useSelector } from 'react-redux'
  import { AppShell } from '../components/layout/AppShell'
  import { IdeaInput } from '../components/validator/IdeaInput'
  import { ResultsPanel } from '../components/validator/ResultsPanel'
  import { Arrow } from '../components/decorative/Arrow'
  import { AuthModal } from '../components/auth/AuthModal'
  import { SignInButton } from '../components/auth/SignInButton'
  import { useAuth } from '../hooks/useAuth'

  export function HomePage() {
    const status = useSelector(s => s.validator.status)
    const { refreshSession } = useAuth()

    // Call refreshSession once on mount (same as App.jsx useEffect)
    // Note: Can be lifted to App.jsx or a layout component if preferred

    return (
      <AppShell>
        <div className="flex flex-col items-center justify-start min-h-screen px-4 py-20 md:px-8 relative">

          {/* Header with SignInButton */}
          <header className="absolute top-4 right-4 md:top-6 md:right-6">
            <SignInButton />
          </header>

          {/* Hero Section */}
          <div className="w-full max-w-2xl text-center mb-20 md:mb-24">
            <h1 className="font-heading text-5xl md:text-6xl text-pencil mb-6 leading-tight">
              Validate Your SaaS Idea
            </h1>
            <p className="font-body text-lg md:text-xl text-pencil leading-relaxed">
              Paste your startup idea and get an honest, investor-grade analysis against our
              30-step framework for building agent-native SaaS businesses.
            </p>
          </div>

          {/* Input Section */}
          <div className="w-full max-w-2xl mb-20 md:mb-24">
            <IdeaInput />
          </div>

          {/* Arrow decoration — hidden on mobile */}
          {status !== 'idle' && (
            <div className="hidden md:flex justify-center mb-12">
              <Arrow direction="down" />
            </div>
          )}

          {/* Results Section */}
          {status !== 'idle' && (
            <div className="w-full">
              <ResultsPanel />
            </div>
          )}

          {/* Footer spacing */}
          <div className="mt-20 md:mt-24" />

        </div>

        {/* Auth Modal */}
        <AuthModal />
      </AppShell>
    )
  }

  Reasoning: Extracted from App.jsx, keeps home page logic in one place. Auth modal still global.
  </action>

  <acceptance_criteria>
  - [ ] client/src/pages/HomePage.jsx exists
  - [ ] Imports and renders IdeaInput, ResultsPanel, Arrow
  - [ ] Shows SignInButton in header
  - [ ] Includes AppShell wrapper
  - [ ] Includes AuthModal
  - [ ] Returns same layout as current App.jsx
  </acceptance_criteria>
</task>

<task id="10-02-04" title="Create HistoryPage.jsx (page shell, to be completed in 10-03)">
  <read_first>
  - client/src/pages/HomePage.jsx — just created, page structure pattern
  - client/src/components/layout/AppShell.jsx — wrapper pattern
  </read_first>

  <action>
  Create client/src/pages/HistoryPage.jsx with shell:

  import { useSelector } from 'react-redux'
  import { AppShell } from '../components/layout/AppShell'
  import { SignInButton } from '../components/auth/SignInButton'
  import { AuthModal } from '../components/auth/AuthModal'
  import { useAuth } from '../hooks/useAuth'

  export function HistoryPage() {
    const user = useSelector(s => s.auth.user)
    const { openModal } = useAuth()

    if (!user) {
      return (
        <AppShell>
          <div className="flex flex-col items-center justify-center min-h-screen px-4 py-20 relative">
            <header className="absolute top-4 right-4 md:top-6 md:right-6">
              <SignInButton />
            </header>

            <div className="w-full max-w-2xl text-center">
              <h1 className="font-heading text-5xl md:text-6xl text-pencil mb-6">
                Your Validation History
              </h1>
              <p className="font-body text-lg md:text-xl text-pencil mb-12 leading-relaxed">
                Sign in to see your saved ideas and past validations.
              </p>
              <button
                onClick={() => openModal('login')}
                className="px-6 py-3 font-body text-lg bg-blue text-white rounded"
              >
                Sign In
              </button>
            </div>
          </div>
          <AuthModal />
        </AppShell>
      )
    }

    return (
      <AppShell>
        <div className="flex flex-col items-center justify-start min-h-screen px-4 py-20 md:px-8 relative">

          {/* Header with SignInButton */}
          <header className="absolute top-4 right-4 md:top-6 md:right-6">
            <SignInButton />
          </header>

          {/* Title */}
          <div className="w-full max-w-4xl text-center mb-12">
            <h1 className="font-heading text-5xl md:text-6xl text-pencil mb-4">
              Your Validation History
            </h1>
          </div>

          {/* Content will be rendered here by useHistory hook in 10-03 */}
          {/* Placeholder for HistoryList component */}

          {/* Footer spacing */}
          <div className="mt-20 md:mt-24" />

        </div>

        {/* Auth Modal */}
        <AuthModal />
      </AppShell>
    )
  }

  Reasoning: Page shell created now; list/sort/infinite scroll logic added in wave 3 (10-03).
  </action>

  <acceptance_criteria>
  - [ ] client/src/pages/HistoryPage.jsx exists
  - [ ] Shows unauthenticated state with "Sign in to see your history" + Sign In button
  - [ ] Shows authenticated header when user exists
  - [ ] Includes AppShell, SignInButton, AuthModal
  </acceptance_criteria>
</task>

<task id="10-02-05" title="Create ResultPage.jsx (page shell, to be completed in 10-04)">
  <read_first>
  - client/src/pages/HomePage.jsx — page pattern
  </read_first>

  <action>
  Create client/src/pages/ResultPage.jsx with shell:

  import { useParams } from 'react-router-dom'
  import { useEffect, useState } from 'react'
  import { AppShell } from '../components/layout/AppShell'
  import { SignInButton } from '../components/auth/SignInButton'
  import { AuthModal } from '../components/auth/AuthModal'
  import { Card } from '../components/ui/Card'

  export function ResultPage() {
    const { id } = useParams()
    const [result, setResult] = useState(null)
    const [loading, setLoading] = useState(true)
    const [error, setError] = useState(null)

    useEffect(() => {
      async function fetchResult() {
        try {
          const res = await fetch(`/api/history/${id}`)
          if (!res.ok) throw new Error('Result not found')
          const data = await res.json()
          setResult(data)
        } catch (err) {
          setError(err.message)
        } finally {
          setLoading(false)
        }
      }
      fetchResult()
    }, [id])

    if (loading) {
      return (
        <AppShell>
          <div className="flex items-center justify-center min-h-screen">
            <p className="font-body text-lg text-pencil">Loading...</p>
          </div>
        </AppShell>
      )
    }

    if (error) {
      return (
        <AppShell>
          <div className="flex flex-col items-center justify-center min-h-screen px-4">
            <h1 className="font-heading text-5xl text-pencil mb-4">Not Found</h1>
            <p className="font-body text-lg text-pencil">{error}</p>
            <a href="/" className="mt-6 text-blue font-body underline">← Back Home</a>
          </div>
        </AppShell>
      )
    }

    return (
      <AppShell>
        <div className="flex flex-col items-center justify-start min-h-screen px-4 py-20 md:px-8 relative">

          {/* Header with SignInButton */}
          <header className="absolute top-4 right-4 md:top-6 md:right-6">
            <SignInButton />
          </header>

          {/* Title */}
          <div className="w-full max-w-4xl text-center mb-12">
            <h1 className="font-heading text-5xl md:text-6xl text-pencil mb-2">
              {result?.title}
            </h1>
          </div>

          {/* Content will be rendered here by split-card components in 10-04 */}
          {/* Placeholder for card rendering */}

          {/* Footer spacing */}
          <div className="mt-20 md:mt-24" />

        </div>

        {/* Auth Modal */}
        <AuthModal />
      </AppShell>
    )
  }

  Reasoning: Fetches result by id from public endpoint, loads split-card rendering logic in wave 4.
  </action>

  <acceptance_criteria>
  - [ ] client/src/pages/ResultPage.jsx exists
  - [ ] Extracts id from URL params via useParams()
  - [ ] Fetches from /api/history/:id on mount (no auth required)
  - [ ] Shows loading state while fetching
  - [ ] Shows 404 error if not found
  - [ ] Renders result.title in header
  - [ ] Includes AppShell, SignInButton, AuthModal
  </acceptance_criteria>
</task>

<task id="10-02-06" title="Update App.jsx to wrap pages with Routes">
  <read_first>
  - client/src/App.jsx — current structure
  - client/src/pages/HomePage.jsx, HistoryPage.jsx, ResultPage.jsx — just created
  - React Router v6 Routes pattern
  </read_first>

  <action>
  Replace client/src/App.jsx with:

  import { useEffect } from 'react'
  import { Routes, Route, useNavigate } from 'react-router-dom'
  import { useDispatch, useSelector } from 'react-redux'
  import { HomePage } from './pages/HomePage'
  import { HistoryPage } from './pages/HistoryPage'
  import { ResultPage } from './pages/ResultPage'
  import { useAuth } from './hooks/useAuth'
  import { setUser, setAuthModalMode, setShowAuthModal } from './store/slices/authSlice'

  export default function App() {
    const dispatch = useDispatch()
    const navigate = useNavigate()
    const { refreshSession, openModal } = useAuth()

    useEffect(() => {
      const params = new URLSearchParams(window.location.search)

      // OAuth callback
      const accessToken = params.get('accessToken')
      if (accessToken) {
        try {
          const payload = JSON.parse(atob(accessToken.split('.')[1]))
          dispatch(setUser({ user: { id: payload.id, email: payload.email }, accessToken }))
        } catch {}
        window.history.replaceState({}, '', '/')
        return
      }

      // Email verified redirect
      if (params.get('verified') === 'true') {
        openModal('login')
        window.history.replaceState({}, '', '/')
        return
      }

      // Password reset redirect
      const resetToken = params.get('reset')
      if (resetToken) {
        sessionStorage.setItem('resetToken', resetToken)
        dispatch(setAuthModalMode('reset'))
        dispatch(setShowAuthModal(true))
        window.history.replaceState({}, '', '/')
        return
      }

      // Auth error
      if (params.get('auth_error')) {
        openModal('login')
        window.history.replaceState({}, '', '/')
        return
      }

      // Normal session restore
      refreshSession()
    }, [dispatch, openModal, refreshSession])

    return (
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/history" element={<HistoryPage />} />
        <Route path="/history/:id" element={<ResultPage />} />
      </Routes>
    )
  }

  Reasoning: App.jsx becomes router wrapper. All URL handling moved here. Page components are dumb route targets.
  </action>

  <acceptance_criteria>
  - [ ] client/src/App.jsx imports Routes, Route, useNavigate from 'react-router-dom'
  - [ ] Imports HomePage, HistoryPage, ResultPage from pages/
  - [ ] Renders Routes with 3 routes: /, /history, /history/:id
  - [ ] Each route element points to correct page component
  - [ ] Keeps URL param handling (OAuth, reset, verify, etc.)
  </acceptance_criteria>
</task>

<task id="10-02-07" title="Add History nav link to SignInButton/header">
  <read_first>
  - client/src/components/auth/SignInButton.jsx — current structure
  - React Router v6 Link component pattern
  </read_first>

  <action>
  Update client/src/components/auth/SignInButton.jsx to include History link:

  Add to imports:
  import { Link } from 'react-router-dom'

  In the JSX, before or alongside the SignInButton, add:

  {user && (
    <Link
      to="/history"
      className="font-body text-lg text-blue hover:text-accent transition-colors mr-4"
    >
      History
    </Link>
  )}

  Or better yet, restructure component to show:
  - History link (if user exists)
  - Sign In button (if no user)
  - Sign Out button (if user exists)

  Reasoning: History page is auth-gated; link only shown when authenticated. Uses React Router Link for client-side navigation.
  </action>

  <acceptance_criteria>
  - [ ] SignInButton or header component imports Link from 'react-router-dom'
  - [ ] History link renders only when user is authenticated
  - [ ] Link href is /history
  - [ ] Link text is "History"
  - [ ] Link styled consistently with design system
  </acceptance_criteria>
</task>

<task id="10-02-08" title="Wire auto-save into useValidate hook">
  <read_first>
  - client/src/hooks/useValidate.js — current validate function
  - client/src/utils/parseResult.js — parseScores function
  - client/src/utils/fetchWithAuth.js — authenticated fetch
  - Phase 10 requirements D-01, D-02
  </read_first>

  <action>
  Update client/src/hooks/useValidate.js to add auto-save after finishValidation:

  import { useDispatch, useSelector } from 'react-redux'
  import { startValidation, startStreaming, appendResult, finishValidation, setError } from '../store/slices/validatorSlice.js'
  import { parseScores } from '../utils/parseResult.js'
  import { fetchWithAuth } from '../utils/fetchWithAuth.js'

  export function useValidate() {
    const dispatch = useDispatch()
    const { idea, status, result, error } = useSelector(s => s.validator)
    const user = useSelector(s => s.auth.user)

    async function validate() {
      dispatch(startValidation())
      try {
        const res = await fetch('/api/validate', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ idea }),
        })
        if (!res.ok) throw new Error('Server error')
        dispatch(startStreaming())
        const reader = res.body.getReader()
        const decoder = new TextDecoder()
        let fullResult = ''
        while (true) {
          const { done, value } = await reader.read()
          if (done) break
          const chunk = decoder.decode(value)
          fullResult += chunk
          dispatch(appendResult(chunk))
        }
        dispatch(finishValidation())

        // Auto-save if user is authenticated
        if (user) {
          const scores = parseScores(fullResult)
          if (scores) {
            try {
              await fetchWithAuth('/api/history', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                  idea_text: idea,
                  markdown_result: fullResult,
                  scores,
                }),
              })
            } catch (saveErr) {
              console.error('Auto-save failed:', saveErr)
              // Silent fail — user's validation is still visible
            }
          }
        }
      } catch (e) {
        dispatch(setError(e.message))
      }
    }

    return { idea, status, result, error, validate }
  }

  Reasoning: After finishValidation, extract scores and POST to /api/history. Only runs if user is authenticated. Silent fail if save errors (validation still succeeds).
  </action>

  <acceptance_criteria>
  - [ ] useValidate hook imports parseScores and fetchWithAuth
  - [ ] After dispatch(finishValidation()), checks if user exists
  - [ ] If user exists, calls parseScores(fullResult)
  - [ ] If scores exist, POSTs to /api/history with idea_text, markdown_result, scores
  - [ ] Uses fetchWithAuth for authenticated request
  - [ ] Silent fail on save error (no dispatch(setError) for save failure)
  - [ ] fullResult accumulates all chunks before save
  </acceptance_criteria>
</task>

<task id="10-02-09" title="Verify routing works end-to-end">
  <read_first>
  - client/src/App.jsx — routes
  - client/src/pages/*.jsx — all page components
  - client/src/main.jsx — BrowserRouter wrapping
  </read_first>

  <action>
  Manual verification steps:
  1. Run npm run dev in client/ and server/ directories
  2. Navigate to http://localhost:5173/ — should see HomePage with input form
  3. Click History link in header (if signed in) — should navigate to /history, show HistoryPage
  4. Manually navigate to http://localhost:5173/history/123 — should show ResultPage shell with loading state (404 expected since no real id)
  5. Check browser console for router errors
  6. Check that auto-save fires (POST to /api/history) when validation completes and user is authenticated

  No code changes in this task; purely verification.
  </action>

  <acceptance_criteria>
  - [ ] http://localhost:5173/ shows HomePage with IdeaInput
  - [ ] http://localhost:5173/history shows HistoryPage (auth state correct)
  - [ ] http://localhost:5173/history/123 shows ResultPage loading/error state
  - [ ] History link appears and links to /history when user is signed in
  - [ ] No browser console errors
  - [ ] Network tab shows POST /api/history after validation completes (if user authenticated)
  </acceptance_criteria>
</task>

## Completion Criteria

- [ ] react-router-dom v6 installed
- [ ] BrowserRouter wraps App in main.jsx
- [ ] App.jsx refactored as router wrapper with Routes
- [ ] HomePage, HistoryPage, ResultPage pages created
- [ ] Three routes working: /, /history, /history/:id
- [ ] History link in header (auth-gated)
- [ ] Auto-save wired into useValidate hook
- [ ] Auto-save only fires for authenticated users
- [ ] Auto-save uses fetchWithAuth and sends idea_text, markdown_result, scores

## must_haves

- React Router v6 properly configured
- Three routes each render correct page component
- Auto-save triggers after validation status becomes 'done'
- Auto-save only runs for authenticated users
- fetchWithAuth used for auto-save POST
- History link only visible when user is logged in
- Page navigation preserves Redux state
