---
plan: 10-01
phase: 10
title: "Database Schema & Backend Routes — Saved Results Infrastructure"
wave: 1
depends_on: [09-03]
status: pending
files_modified:
  - server/db/schema.sql
  - server/middleware/optionalAuth.js
  - server/routes/history.js
  - server/index.js
---

# Plan 10-01: Database & Backend Infrastructure

## Goal

Create the PostgreSQL `saved_results` table with soft-delete support, implement 5 backend routes for auto-save, list, fetch, rename, and delete operations, and add optional-auth middleware for public result viewing.

## Requirements Met

- D-03: Table with id, user_id, title, idea_text, markdown_result, scores (JSONB), created_at, updated_at, deleted_at
- D-06: Soft delete via deleted_at timestamp
- D-08: All queries exclude deleted_at IS NULL
- D-01/D-02: Auto-save triggers on validation completion
- D-04: Default title = first 6 words; AI title generated async
- Backend routes: POST /api/history, GET /api/history, GET /api/history/:id, PATCH /api/history/:id/title, DELETE /api/history/:id

## Tasks

<task id="10-01-01" title="Create saved_results table migration">
  <read_first>
  - server/db/schema.sql — existing table patterns (users, refresh_tokens)
  </read_first>

  <action>
  Append to server/db/schema.sql after the refresh_tokens table:

  CREATE TABLE IF NOT EXISTS saved_results (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    idea_text TEXT NOT NULL,
    markdown_result TEXT NOT NULL,
    scores JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    deleted_at TIMESTAMPTZ DEFAULT NULL,
    CONSTRAINT scores_valid CHECK (scores::text ~ '^\{.*\}$')
  );

  CREATE INDEX IF NOT EXISTS saved_results_user_id_idx ON saved_results(user_id) WHERE deleted_at IS NULL;
  CREATE INDEX IF NOT EXISTS saved_results_created_at_idx ON saved_results(created_at DESC) WHERE deleted_at IS NULL;

  Reasoning: UUID primary key matches pattern, user_id enforces ownership, JSONB scores allows flexible storage, deleted_at enables soft delete, indexes optimize list queries by user and date.
  </action>

  <acceptance_criteria>
  - [ ] server/db/schema.sql contains CREATE TABLE saved_results with all 9 columns
  - [ ] deleted_at column has DEFAULT NULL
  - [ ] scores column is JSONB with CHECK constraint
  - [ ] user_id has ON DELETE CASCADE reference to users
  - [ ] Two indexes created: one on (user_id) WHERE deleted_at IS NULL, one on (created_at DESC) WHERE deleted_at IS NULL
  </acceptance_criteria>
</task>

<task id="10-01-02" title="Create optionalAuth middleware">
  <read_first>
  - server/middleware/requireAuth.js — auth token extraction pattern
  - server/utils/jwt.js — verifyAccessToken function
  </read_first>

  <action>
  Create new file server/middleware/optionalAuth.js with:

  import { verifyAccessToken } from '../utils/jwt.js'

  export function optionalAuth(req, res, next) {
    const authHeader = req.headers.authorization
    if (authHeader?.startsWith('Bearer ')) {
      const token = authHeader.slice(7)
      try {
        req.user = verifyAccessToken(token)
      } catch {
        req.user = null
      }
    } else {
      req.user = null
    }
    next()
  }

  Reasoning: Unlike requireAuth, optionalAuth attempts to extract and verify token but continues even if absent or invalid. Used for public result viewing where auth is optional.
  </action>

  <acceptance_criteria>
  - [ ] File server/middleware/optionalAuth.js exists
  - [ ] Function exports optionalAuth
  - [ ] Calls next() regardless of token presence
  - [ ] Sets req.user to verifyAccessToken result or null
  </acceptance_criteria>
</task>

<task id="10-01-03" title="Create POST /api/history route (auto-save)">
  <read_first>
  - server/routes/auth.js — route handler pattern (async, error handling, pool queries)
  - server/db/init.js — pool export
  - client/src/utils/parseResult.js — parseScores function signature
  </read_first>

  <action>
  Create new file server/routes/history.js with POST /api/history handler:

  export async function saveResultRoute(req, res) {
    if (!req.user) {
      return res.status(401).json({ error: 'Unauthorized' })
    }

    const { idea_text, markdown_result, scores } = req.body
    if (!idea_text || !markdown_result || !scores) {
      return res.status(400).json({ error: 'Missing required fields' })
    }

    try {
      // Generate default title from first 6 words
      const defaultTitle = idea_text
        .split(' ')
        .slice(0, 6)
        .join(' ')
        .replace(/[^a-z0-9\s]/gi, '')
        .substring(0, 50)

      const { rows } = await pool.query(
        `INSERT INTO saved_results (user_id, title, idea_text, markdown_result, scores)
         VALUES ($1, $2, $3, $4, $5)
         RETURNING id, title, created_at`,
        [req.user.id, defaultTitle, idea_text, markdown_result, JSON.stringify(scores)]
      )

      const result = rows[0]

      // Fire async title generation via Claude (non-blocking)
      generateAITitle(result.id, idea_text, req.user.id).catch(err => {
        console.error('Title generation failed:', err)
      })

      res.status(201).json({
        id: result.id,
        title: result.title,
        createdAt: result.created_at,
      })
    } catch (err) {
      console.error('Save failed:', err)
      res.status(500).json({ error: 'Failed to save result' })
    }
  }

  async function generateAITitle(resultId, ideaText, userId) {
    const client = new Anthropic()
    try {
      const response = await client.messages.create({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 50,
        system: 'You are a naming expert. Generate a 3-4 word title for a startup idea. Return ONLY the title, no quotes or punctuation.',
        messages: [{ role: 'user', content: ideaText }],
      })
      const aiTitle = response.content[0].text.trim().substring(0, 100)
      await pool.query(
        'UPDATE saved_results SET title = $1, updated_at = now() WHERE id = $2 AND user_id = $3',
        [aiTitle, resultId, userId]
      )
    } catch (err) {
      console.error('generateAITitle error:', err)
    }
  }

  Reasoning: Default title + AI title generation (async, non-blocking). Prevents save latency. AI title updates existing record once ready.
  </action>

  <acceptance_criteria>
  - [ ] server/routes/history.js exports saveResultRoute
  - [ ] Handler validates req.user exists (401)
  - [ ] Validates idea_text, markdown_result, scores present (400)
  - [ ] Extracts first 6 words from idea_text for defaultTitle
  - [ ] INSERTs into saved_results with user_id, title, idea_text, markdown_result, scores (stringified JSON)
  - [ ] Returns { id, title, createdAt } with 201 status
  - [ ] generateAITitle function exists and runs async
  - [ ] generateAITitle UPDATEs title via Claude API call
  </acceptance_criteria>
</task>

<task id="10-01-04" title="Create GET /api/history route (list with cursor pagination)">
  <read_first>
  - server/routes/history.js — saveResultRoute pattern just created
  - server/middleware/requireAuth.js — auth pattern
  </read_first>

  <action>
  Append to server/routes/history.js:

  export async function listHistoryRoute(req, res) {
    if (!req.user) {
      return res.status(401).json({ error: 'Unauthorized' })
    }

    const { sort = 'date', cursor } = req.query
    const LIMIT = 10

    try {
      let query, params

      if (sort === 'score') {
        query = `
          SELECT id, title, idea_text, markdown_result, scores, created_at
          FROM saved_results
          WHERE user_id = $1 AND deleted_at IS NULL
          ORDER BY (scores->>'weighted')::float DESC, created_at DESC
          LIMIT $2
        `
        params = [req.user.id, LIMIT + 1]
      } else {
        // date (default)
        query = `
          SELECT id, title, idea_text, markdown_result, scores, created_at
          FROM saved_results
          WHERE user_id = $1 AND deleted_at IS NULL
          ORDER BY created_at DESC
          LIMIT $2
        `
        params = [req.user.id, LIMIT + 1]
      }

      const { rows } = await pool.query(query, params)
      const hasMore = rows.length > LIMIT
      const items = rows.slice(0, LIMIT).map(r => ({
        id: r.id,
        title: r.title,
        idea_text: r.idea_text,
        scores: r.scores,
        created_at: r.created_at,
      }))

      res.json({ items, hasMore })
    } catch (err) {
      console.error('List history error:', err)
      res.status(500).json({ error: 'Failed to fetch history' })
    }
  }

  Reasoning: Simple cursor-free pagination (load LIMIT+1 to detect hasMore). Sort by date (newest first) or score (highest first). Always exclude deleted_at IS NULL.
  </action>

  <acceptance_criteria>
  - [ ] listHistoryRoute exported from server/routes/history.js
  - [ ] Handler validates req.user (401)
  - [ ] Query params: sort (date|score), cursor (optional)
  - [ ] Default sort is 'date'
  - [ ] For sort=score: ORDER BY (scores->>'weighted')::float DESC
  - [ ] For sort=date: ORDER BY created_at DESC
  - [ ] All queries include WHERE deleted_at IS NULL
  - [ ] Returns { items: [...], hasMore: bool }
  - [ ] Items array limited to 10, fetches 11 to detect hasMore
  </acceptance_criteria>
</task>

<task id="10-01-05" title="Create GET /api/history/:id route (public view with optionalAuth)">
  <read_first>
  - server/middleware/optionalAuth.js — just created
  - server/routes/history.js — pattern
  </read_first>

  <action>
  Append to server/routes/history.js:

  export async function getResultRoute(req, res) {
    const { id } = req.params

    try {
      const { rows } = await pool.query(
        `SELECT id, user_id, title, idea_text, markdown_result, scores, created_at
         FROM saved_results
         WHERE id = $1 AND deleted_at IS NULL`,
        [id]
      )

      if (!rows.length) {
        return res.status(404).json({ error: 'Result not found' })
      }

      const result = rows[0]
      const isOwner = req.user?.id === result.user_id

      res.json({
        id: result.id,
        title: result.title,
        idea_text: result.idea_text,
        markdown_result: result.markdown_result,
        scores: result.scores,
        created_at: result.created_at,
        isOwner,
      })
    } catch (err) {
      console.error('Get result error:', err)
      res.status(500).json({ error: 'Failed to fetch result' })
    }
  }

  Reasoning: Public route (used with optionalAuth middleware). Sets isOwner flag in response so frontend knows whether to show delete/rename. Returns 404 if not found or soft-deleted.
  </action>

  <acceptance_criteria>
  - [ ] getResultRoute exported from server/routes/history.js
  - [ ] No req.user check — works with optionalAuth
  - [ ] Query by id, WHERE deleted_at IS NULL
  - [ ] Returns 404 if not found
  - [ ] Response includes isOwner: boolean (true if req.user?.id === result.user_id)
  - [ ] Response includes all fields: id, title, idea_text, markdown_result, scores, created_at
  </acceptance_criteria>
</task>

<task id="10-01-06" title="Create PATCH /api/history/:id/title route (ownership check)">
  <read_first>
  - server/routes/history.js — pattern
  - server/middleware/requireAuth.js — auth validation
  </read_first>

  <action>
  Append to server/routes/history.js:

  export async function updateTitleRoute(req, res) {
    if (!req.user) {
      return res.status(401).json({ error: 'Unauthorized' })
    }

    const { id } = req.params
    const { title } = req.body

    if (!title || title.trim().length === 0) {
      return res.status(400).json({ error: 'Title cannot be empty' })
    }

    try {
      const { rows } = await pool.query(
        'SELECT user_id FROM saved_results WHERE id = $1 AND deleted_at IS NULL',
        [id]
      )

      if (!rows.length) {
        return res.status(404).json({ error: 'Result not found' })
      }

      if (rows[0].user_id !== req.user.id) {
        return res.status(403).json({ error: 'Forbidden' })
      }

      const truncatedTitle = title.trim().substring(0, 255)
      const updateRows = await pool.query(
        'UPDATE saved_results SET title = $1, updated_at = now() WHERE id = $2 RETURNING title, updated_at',
        [truncatedTitle, id]
      )

      res.json({
        id,
        title: updateRows.rows[0].title,
        updated_at: updateRows.rows[0].updated_at,
      })
    } catch (err) {
      console.error('Update title error:', err)
      res.status(500).json({ error: 'Failed to update title' })
    }
  }

  Reasoning: Requires auth, ownership check, and validates title non-empty. Truncates to 255 chars to fit column constraint.
  </action>

  <acceptance_criteria>
  - [ ] updateTitleRoute exported from server/routes/history.js
  - [ ] Validates req.user (401)
  - [ ] Validates title is non-empty (400)
  - [ ] Checks record exists and deleted_at IS NULL (404)
  - [ ] Checks user_id === req.user.id (403 if not)
  - [ ] Truncates title to 255 chars
  - [ ] UPDATEs title and updated_at
  - [ ] Returns { id, title, updated_at }
  </acceptance_criteria>
</task>

<task id="10-01-07" title="Create DELETE /api/history/:id route (soft delete with ownership)">
  <read_first>
  - server/routes/history.js — pattern
  </read_first>

  <action>
  Append to server/routes/history.js:

  export async function deleteResultRoute(req, res) {
    if (!req.user) {
      return res.status(401).json({ error: 'Unauthorized' })
    }

    const { id } = req.params

    try {
      const { rows } = await pool.query(
        'SELECT user_id FROM saved_results WHERE id = $1 AND deleted_at IS NULL',
        [id]
      )

      if (!rows.length) {
        return res.status(404).json({ error: 'Result not found' })
      }

      if (rows[0].user_id !== req.user.id) {
        return res.status(403).json({ error: 'Forbidden' })
      }

      await pool.query(
        'UPDATE saved_results SET deleted_at = now() WHERE id = $1',
        [id]
      )

      res.json({ message: 'Result deleted' })
    } catch (err) {
      console.error('Delete error:', err)
      res.status(500).json({ error: 'Failed to delete result' })
    }
  }

  Reasoning: Soft delete only — sets deleted_at timestamp, preserves data. Checks ownership.
  </action>

  <acceptance_criteria>
  - [ ] deleteResultRoute exported from server/routes/history.js
  - [ ] Validates req.user (401)
  - [ ] Checks record exists and deleted_at IS NULL (404)
  - [ ] Checks user_id === req.user.id (403 if not)
  - [ ] UPDATEs deleted_at = now() (not hard delete)
  - [ ] Returns { message: 'Result deleted' }
  </acceptance_criteria>
</task>

<task id="10-01-08" title="Mount all 5 history routes in server/index.js">
  <read_first>
  - server/index.js — existing route mounting pattern
  - server/middleware/optionalAuth.js — just created
  - server/routes/history.js — all 5 handlers just created
  - server/middleware/requireAuth.js — existing auth pattern
  </read_first>

  <action>
  Update server/index.js to import and mount history routes:

  Add to imports at top:
  import { saveResultRoute, listHistoryRoute, getResultRoute, updateTitleRoute, deleteResultRoute } from './routes/history.js'
  import { optionalAuth } from './middleware/optionalAuth.js'
  import { requireAuth } from './middleware/requireAuth.js'

  Add after existing auth route mounts (around line 42):
  app.post('/api/history', requireAuth, saveResultRoute)
  app.get('/api/history', requireAuth, listHistoryRoute)
  app.get('/api/history/:id', optionalAuth, getResultRoute)
  app.patch('/api/history/:id/title', requireAuth, updateTitleRoute)
  app.delete('/api/history/:id', requireAuth, deleteResultRoute)

  Reasoning: POST/GET list/PATCH/DELETE require auth; GET by id uses optionalAuth for public access.
  </action>

  <acceptance_criteria>
  - [ ] server/index.js imports saveResultRoute, listHistoryRoute, getResultRoute, updateTitleRoute, deleteResultRoute from routes/history.js
  - [ ] server/index.js imports optionalAuth from middleware/optionalAuth.js
  - [ ] All 5 routes mounted:
    - POST /api/history with requireAuth
    - GET /api/history with requireAuth
    - GET /api/history/:id with optionalAuth
    - PATCH /api/history/:id/title with requireAuth
    - DELETE /api/history/:id with requireAuth
  </acceptance_criteria>
</task>

<task id="10-01-09" title="Import Anthropic SDK in history.js for title generation">
  <read_first>
  - server/routes/validate.js — Anthropic SDK import pattern
  - server/routes/history.js — generateAITitle function created in task 10-01-03
  </read_first>

  <action>
  Add to top of server/routes/history.js (above exports):

  import Anthropic from '@anthropic-ai/sdk'
  import { pool } from '../db/init.js'

  Reasoning: Required for generateAITitle function to call Claude API.
  </action>

  <acceptance_criteria>
  - [ ] server/routes/history.js imports Anthropic from '@anthropic-ai/sdk'
  - [ ] server/routes/history.js imports pool from '../db/init.js'
  </acceptance_criteria>
</task>

## Completion Criteria

- [ ] Database migration: `saved_results` table exists with all columns and indexes
- [ ] optionalAuth middleware created and functional
- [ ] All 5 history routes implemented (save, list, fetch, rename, delete)
- [ ] Routes mounted in server/index.js with correct auth middleware
- [ ] Auto-save POST endpoint accepts { idea_text, markdown_result, scores } and returns { id, title, createdAt }
- [ ] List endpoint filters by user_id, excludes deleted_at IS NULL, supports sort=date|score
- [ ] Public fetch endpoint works without auth, includes isOwner flag
- [ ] Rename and delete endpoints check ownership and return 403 if not owner
- [ ] Soft delete only — sets deleted_at, does not remove rows

## must_haves

- Database schema includes saved_results table with soft-delete support
- All 5 routes implemented with proper error handling
- optionalAuth middleware works with optional bearer tokens
- Routes correctly validate user ownership before modify/delete
- All queries exclude deleted_at IS NULL to hide soft-deleted records
- Auto-save fires on validation completion and returns result id
