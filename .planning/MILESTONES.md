# Milestones

## v1.0 MVP (Shipped: 2026-03-22)

**Phases completed:** 14 phases, 32 plans, 63 tasks

**Key accomplishments:**

- Monorepo scaffold — Express 5 + Vite + React 19 + Tailwind v3 with full custom design token system
- Streaming Claude API integration — live 30-step framework analysis via ReadableStream with Redux state machine
- Hand-drawn sketchbook design system — wobbly borders, hard offset shadows, Kalam/Patrick Hand fonts across all UI primitives
- Visual scorecard — phase breakdown scoring with weighted total (30%/25%/35%/10%) and colour-coded verdict badge
- Full authentication — email/password + Google/GitHub OAuth, JWT rotation, bcrypt, auth-gated Validate action
- PostgreSQL persistence — saved validation history with infinite scroll, inline title editing, and public sharing links
- Split-card results layout — animated IdeaSummary, Commentary, and Verdict cards with streaming indicator
- Framework page — dedicated `/framework` route showing all 30 steps grouped by phase with navbar integration
- ResultPage refactor — clean architecture: <200 lines, extracted useHistoryResult hook + TitleHeader + ActionButtons + DeleteConfirmModal + formatResultDate utility

---
