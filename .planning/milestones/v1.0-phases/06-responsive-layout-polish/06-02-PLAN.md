---
phase: "06"
plan: "06-02"
wave: 2
autonomous: true
subsystem: client
tags: [responsive, polish, accessibility]
requires: [06-01-PLAN]
provides: []
affects:
  - client/src/components/ui/Button.jsx
  - client/src/components/ui/TextArea.jsx
  - client/src/components/validator/IdeaInput.jsx
  - client/src/components/decorative/Arrow.jsx
requirements: []
objective: Polish responsive layout with fine-tuned spacing, button touch target sizing, accessibility compliance, and visual design consistency across all screen sizes
key-files:
  created: []
  modified:
    - client/src/components/ui/Button.jsx
    - client/src/components/ui/TextArea.jsx
    - client/src/components/validator/IdeaInput.jsx
    - client/src/components/decorative/Arrow.jsx
must_haves:
  - Button component has h-12 minimum (48px touch target)
  - TextArea maintains full wobbly border and focus styles at all viewports
  - IdeaInput form has accessible label (htmlFor) associated with textarea (id)
  - IdeaInput form has aria-busy on submit button during loading
  - Arrow decoration uses hidden md:block (visible on tablet+, hidden on mobile)
  - All focus states use blue ring for keyboard navigation
  - Disabled button state clearly visible (opacity change)
---

# Phase 6 Plan 02: Responsive Polish & Accessibility

## Objective

Refine responsive layout details and ensure accessibility compliance. Verify button sizing meets touch target requirements (minimum 48px = h-12), confirm wobbly borders and design system render at all screen sizes, validate keyboard navigation and focus states.

## Context

Plan 01 established the core page layout. Plan 02 handles the refinement pass:
1. **Button Sizing**: h-12 (48px) minimum for mobile touch targets (WCAG 2.5.5)
2. **Form Accessibility**: label/input association, aria-busy on loading state
3. **TextArea**: wobbly borders confirmed at all viewports
4. **Arrow**: correct responsive hiding (hidden md:block)

## Tasks

### Task 06-02-01: Update Button component for touch-friendly sizing

<read_first>
- client/src/components/ui/Button.jsx
- CLAUDE.md (Button Component API section)
- client/tailwind.config.js (verify shadow and color token names)
</read_first>

<action>
Update `client/src/components/ui/Button.jsx` to ensure minimum 48px touch target height and correct hover/active states. The component must match exactly:

```jsx
export function Button({
  variant = 'primary',
  disabled = false,
  children,
  className = '',
  ...props
}) {
  const baseClasses = 'font-body text-lg md:text-xl h-12 md:h-14 px-6 md:px-8 transition-all duration-100 inline-flex items-center justify-center font-medium'

  const variantClasses = {
    primary: `
      bg-paper text-pencil shadow-hard
      hover:bg-accent hover:text-white
      active:translate-x-1 active:translate-y-1 active:shadow-none
      disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:bg-paper disabled:hover:text-pencil
    `.trim(),
    secondary: `
      bg-muted text-pencil shadow-hard
      hover:bg-blue hover:text-white
      active:translate-x-1 active:translate-y-1 active:shadow-none
      disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:bg-muted disabled:hover:text-pencil
    `.trim(),
  }

  return (
    <button
      disabled={disabled}
      className={`${baseClasses} ${variantClasses[variant] || variantClasses.primary} ${className}`}
      style={{
        borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px',
        WebkitTapHighlightColor: 'transparent',
      }}
      {...props}
    >
      {children}
    </button>
  )
}
```
</action>

<acceptance_criteria>
- `grep "h-12 md:h-14" client/src/components/ui/Button.jsx` returns 1 match
- `grep "255px 15px 225px 15px" client/src/components/ui/Button.jsx` returns 1 match (wobbly border-radius)
- `grep "disabled:opacity-50" client/src/components/ui/Button.jsx` returns 2 matches (primary + secondary)
- `grep "active:translate-x-1" client/src/components/ui/Button.jsx` returns 2 matches (press effect)
- `grep "hover:bg-accent" client/src/components/ui/Button.jsx` returns 1 match (primary red hover)
- `grep "hover:bg-blue" client/src/components/ui/Button.jsx` returns 1 match (secondary blue hover)
- `grep "WebkitTapHighlightColor" client/src/components/ui/Button.jsx` returns 1 match
- `grep "items-center justify-center" client/src/components/ui/Button.jsx` returns 1 match
- No `rounded-*` Tailwind classes used (only inline border-radius style)
</acceptance_criteria>

---

### Task 06-02-02: Verify TextArea maintains wobbly borders and focus states

<read_first>
- client/src/components/ui/TextArea.jsx
- CLAUDE.md (TextArea Component section)
</read_first>

<action>
Verify `client/src/components/ui/TextArea.jsx` has the full wobbly border inline style and all required classes. If it does not match, update it to:

```jsx
export function TextArea({
  value = '',
  onChange,
  placeholder = '',
  disabled = false,
  className = '',
  id,
  ...props
}) {
  return (
    <textarea
      id={id}
      value={value}
      onChange={onChange}
      placeholder={placeholder}
      disabled={disabled}
      className={`
        w-full font-body text-lg text-pencil
        bg-white border-2 border-pencil
        placeholder:text-muted placeholder:opacity-60
        focus:border-blue focus:ring-2 focus:ring-blue/20
        focus:outline-none
        disabled:bg-muted disabled:opacity-50 disabled:cursor-not-allowed
        resize-none
        p-4
        ${className}
      `}
      style={{
        borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px',
      }}
      {...props}
    />
  )
}
```
</action>

<acceptance_criteria>
- `grep "255px 15px 225px 15px" client/src/components/ui/TextArea.jsx` returns 1 match (wobbly border-radius)
- `grep "focus:border-blue" client/src/components/ui/TextArea.jsx` returns 1 match
- `grep "focus:ring-2" client/src/components/ui/TextArea.jsx` returns 1 match
- `grep "resize-none" client/src/components/ui/TextArea.jsx` returns 1 match
- `grep "disabled:opacity-50" client/src/components/ui/TextArea.jsx` returns 1 match
- `grep "id=" client/src/components/ui/TextArea.jsx` returns 1 match (id prop forwarded)
- No `rounded-*` Tailwind classes (only inline border-radius)
</acceptance_criteria>

---

### Task 06-02-03: Update IdeaInput with form accessibility and responsive spacing

<read_first>
- client/src/components/validator/IdeaInput.jsx
- CLAUDE.md (Architecture Rules, Typography Rules)
- client/src/store/slices/validatorSlice.js
- client/src/hooks/useValidate.js
</read_first>

<action>
Update `client/src/components/validator/IdeaInput.jsx` to add form accessibility (label/input association, aria-busy) and responsive spacing. Replace the full component with:

```jsx
import { useDispatch, useSelector } from 'react-redux'
import { setIdea } from '../../store/slices/validatorSlice'
import { useValidate } from '../../hooks/useValidate'
import { Button } from '../ui/Button'
import { TextArea } from '../ui/TextArea'
import { Card } from '../ui/Card'

export function IdeaInput() {
  const dispatch = useDispatch()
  const { idea, status } = useSelector(s => s.validator)
  const { validate } = useValidate()

  const isLoading = status === 'loading' || status === 'streaming'

  function handleSubmit(e) {
    e.preventDefault()
    if (!isLoading) validate()
  }

  return (
    <Card decoration="tape" rotate={-1} className="w-full">
      <form onSubmit={handleSubmit} className="flex flex-col gap-4 md:gap-6">
        <label htmlFor="idea-input" className="font-heading text-2xl md:text-3xl text-pencil">
          Your SaaS Idea
        </label>
        <TextArea
          id="idea-input"
          value={idea}
          onChange={e => dispatch(setIdea(e.target.value))}
          placeholder="Describe your SaaS idea in a few sentences. Include the target customer, the problem, and your proposed solution..."
          disabled={isLoading}
          className="min-h-[180px] md:min-h-[220px]"
        />
        <Button
          type="submit"
          variant="primary"
          disabled={isLoading || idea.trim().length < 20}
          aria-busy={isLoading}
        >
          {isLoading ? 'Validating...' : 'Validate Idea'}
        </Button>
      </form>
    </Card>
  )
}
```
</action>

<acceptance_criteria>
- `grep "htmlFor=\"idea-input\"" client/src/components/validator/IdeaInput.jsx` returns 1 match
- `grep "id=\"idea-input\"" client/src/components/validator/IdeaInput.jsx` returns 1 match (textarea has id matching label)
- `grep "aria-busy" client/src/components/validator/IdeaInput.jsx` returns 1 match
- `grep "md:gap-6" client/src/components/validator/IdeaInput.jsx` returns 1 match (responsive gap)
- `grep "md:text-3xl" client/src/components/validator/IdeaInput.jsx` returns 1 match (responsive heading)
- `grep "min-h-\[220px\]" client/src/components/validator/IdeaInput.jsx` returns 1 match
- `grep "idea.trim().length < 20" client/src/components/validator/IdeaInput.jsx` returns 1 match (server-side min length enforcement on client)
- No `useState` in this component (all state via Redux)
- Form has `onSubmit={handleSubmit}` (not onClick on button)
</acceptance_criteria>

---

### Task 06-02-04: Verify Arrow decoration uses hidden md:block

<read_first>
- client/src/components/decorative/Arrow.jsx
- CLAUDE.md (Responsive Rules section)
</read_first>

<action>
Verify `client/src/components/decorative/Arrow.jsx` uses `className="hidden md:block"` as its outer wrapper class so it is invisible on mobile and visible at the md: breakpoint (768px+). If the current implementation does not include this class as a default, update the component:

The component should have `hidden md:block` on its root SVG element or wrapper div. If the component currently accepts className and the parent (App.jsx) controls visibility, that is also acceptable — but App.jsx must use `hidden md:flex` or `hidden md:block` when rendering Arrow.

Verify one of these is true:
1. Arrow.jsx root element has `className="hidden md:block ..."`
2. App.jsx wraps Arrow with `<div className="hidden md:flex ...">` (already the case from 06-01-01)

If neither, add `hidden md:block` to Arrow.jsx's root element className.
</action>

<acceptance_criteria>
- `grep "hidden md:" client/src/components/decorative/Arrow.jsx` OR `grep "hidden md:" client/src/App.jsx` returns at least 1 match
- Arrow is not visible on mobile: confirmed by `hidden` class being applied at mobile breakpoint
- `grep "stroke=\"#2d2d2d\"\|stroke='#2d2d2d'" client/src/components/decorative/Arrow.jsx` returns at least 1 match (pencil color strokes)
- `grep "strokeWidth=\"2.5\"\|stroke-width=\"2.5\"" client/src/components/decorative/Arrow.jsx` returns at least 1 match
- SVG viewBox attribute present
</acceptance_criteria>

---

## Verification Criteria

After both plans execute:
- `cd client && npm run build` exits 0 with no errors
- `grep "h-12" client/src/components/ui/Button.jsx` returns 1 match
- `grep "htmlFor" client/src/components/validator/IdeaInput.jsx` returns 1 match
- `grep "aria-busy" client/src/components/validator/IdeaInput.jsx` returns 1 match
- App.jsx renders without console.error in browser
- Mobile viewport (375px): single column layout, no horizontal scroll
- Tablet viewport (768px): decorative elements appear, spacing increases
- Desktop viewport (1920px): all breakpoints apply correctly

## Commits

```
git add client/src/components/ui/Button.jsx
git commit -m "feat(06-02-01): ensure Button meets 48px touch target with h-12 md:h-14"

git add client/src/components/ui/TextArea.jsx
git commit -m "feat(06-02-02): verify TextArea wobbly borders and focus styles"

git add client/src/components/validator/IdeaInput.jsx
git commit -m "feat(06-02-03): add form accessibility labels and responsive spacing to IdeaInput"

git add client/src/components/decorative/Arrow.jsx
git commit -m "feat(06-02-04): verify Arrow hidden md:block responsive behavior"
```
