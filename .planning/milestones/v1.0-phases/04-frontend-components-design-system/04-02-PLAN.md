---
phase: "04"
plan: "04-02"
wave: 2
autonomous: true
subsystem: client
tags: [ui-components, button, card, textarea, scorebar]
depends_on: [04-01]
files_modified:
  - client/src/components/ui/Button.jsx
  - client/src/components/ui/Card.jsx
  - client/src/components/ui/TextArea.jsx
  - client/src/components/ui/ScoreBar.jsx
requirements:
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
---

# Plan 04-02: UI Primitives Components

<objective>
Build the foundational UI component library — four reusable primitives that apply the hand-drawn design system. Each component is dumb (pure props, no Redux), styled consistently with wobbly borders, hard shadows, and Patrick Hand font. These components are building blocks that wave 3 feature components depend on.
</objective>

<must_haves>
- client/src/components/ui/Button.jsx exists and exports Button as named export
- Button has inline borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Button uses shadow-hard class and font-body class
- Button primary variant uses bg-white and hover:bg-accent
- Button secondary variant uses bg-muted and hover:bg-blue
- Button has h-12 class for touch targets
- client/src/components/ui/Card.jsx exists and exports Card as named export
- Card has inline borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Card accepts decoration prop ('tape'|'tack'|'none') and rotate prop (number)
- Card tape decoration: gray bar at top-center with opacity-40
- Card tack decoration: red circle at top-center with bg-accent color
- client/src/components/ui/TextArea.jsx exists and exports TextArea as named export
- TextArea has inline borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- TextArea uses resize-none, font-body, text-pencil classes
- TextArea focus state uses focus:border-blue and focus:ring-2
- client/src/components/ui/ScoreBar.jsx exists and exports ScoreBar as named export
- ScoreBar renders exactly 5 circles
- ScoreBar filled circles (num <= score) have backgroundColor: '#2d2d2d' and boxShadow: '4px 4px 0px 0px #2d2d2d'
- ScoreBar empty circles have backgroundColor: '#e5e0d8'
- ScoreBar container row has inline transform: 'rotate(-1deg)'
- All components use text-pencil color, never pure black (#000000)
</must_haves>

<tasks>

<task id="04-02-01">
<name>Create Button component with primary and secondary variants</name>
<action>
Create `client/src/components/ui/Button.jsx` with primary and secondary variants.

Component signature: `Button({ variant = 'primary', onClick, disabled = false, children, className = '' })`

From CLAUDE.md Button Component API section:
- Primary: white bg (bg-white) → red fill on hover (hover:bg-accent hover:text-white)
- Secondary: muted bg (bg-muted) → blue fill on hover (hover:bg-blue hover:text-white)
- Font: font-body class (Patrick Hand)
- Minimum height: h-12 (48px touch target)
- Wobbly inline border-radius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Hard shadow: shadow-hard
- Text: text-pencil
- Active state (when not disabled): translate to simulate press
- Disabled: opacity-50, cursor-not-allowed

```jsx
export function Button({ variant = 'primary', onClick, disabled = false, type = 'button', children, className = '' }) {
  const base = 'font-body text-pencil text-lg md:text-2xl h-12 px-6 transition-all duration-100 border-2 border-pencil shadow-hard'

  const variants = {
    primary: 'bg-white hover:bg-accent hover:text-white',
    secondary: 'bg-muted hover:bg-blue hover:text-white',
  }

  const activeClasses = !disabled ? 'active:translate-x-[2px] active:translate-y-[2px] active:shadow-none' : ''
  const disabledClasses = disabled ? 'opacity-50 cursor-not-allowed' : 'cursor-pointer'

  return (
    <button
      type={type}
      onClick={onClick}
      disabled={disabled}
      style={{ borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px' }}
      className={`${base} ${variants[variant] || variants.primary} ${activeClasses} ${disabledClasses} ${className}`}
    >
      {children}
    </button>
  )
}
```

Create the `client/src/components/ui/` directory if it doesn't exist.
</action>
<read_first>
- CLAUDE.md (Button Component API section)
- client/tailwind.config.js (verify shadow-hard, color tokens: pencil, accent, muted, blue exist)
</read_first>
<acceptance_criteria>
- client/src/components/ui/Button.jsx exists
- Exports Button as named export
- Accepts variant prop (defaults to 'primary')
- Accepts onClick, disabled, type, children, className props
- Has inline style borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Uses font-body class
- Uses text-pencil class
- Uses h-12 class
- Uses shadow-hard class
- Uses border-2 and border-pencil classes
- Primary variant contains bg-white and hover:bg-accent
- Secondary variant contains bg-muted and hover:bg-blue
- Both variants have hover:text-white
- Has active:translate-x-[2px] when not disabled
- Has active:translate-y-[2px] when not disabled
- Has active:shadow-none when not disabled
- Has opacity-50 when disabled is true
- Has cursor-not-allowed when disabled is true
</acceptance_criteria>
</task>

<task id="04-02-02">
<name>Create Card component with tape and tack decorations</name>
<action>
Create `client/src/components/ui/Card.jsx` with tape/tack decoration and rotate capability.

Component signature: `Card({ decoration = 'none', rotate = 0, children, className = '' })`

From CLAUDE.md Card Component API section:
- decoration: 'tape' | 'tack' | 'none'
- rotate: number representing degrees
- Wobbly inline border-radius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Inline transform for rotation
- Base styles: bg-white, shadow-hard, p-6, relative

Decoration implementations:
- tape: Absolutely positioned gray bar at top-center (left-1/2 -translate-x-1/2), 48px wide, 8px tall, bg-gray-400 opacity-40, -top-2, slight 1deg rotation
- tack: Absolutely positioned red circle at top-center (left-1/2 -translate-x-1/2), 12px x 12px, rounded-full, bg-accent (#ff4d4d), shadow-hardRed, -top-2

```jsx
export function Card({ decoration = 'none', rotate = 0, children, className = '' }) {
  return (
    <div
      style={{
        borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px',
        transform: `rotate(${rotate}deg)`,
      }}
      className={`bg-white shadow-hard p-6 relative ${className}`}
    >
      {decoration === 'tape' && (
        <div
          style={{ transform: 'rotate(1deg)' }}
          className="absolute -top-2 left-1/2 -translate-x-1/2 w-12 h-2 bg-gray-400 opacity-40"
        />
      )}
      {decoration === 'tack' && (
        <div
          className="absolute -top-2 left-1/2 -translate-x-1/2 w-3 h-3 rounded-full bg-accent shadow-hardRed"
        />
      )}
      {children}
    </div>
  )
}
```
</action>
<read_first>
- CLAUDE.md (Card Component API section)
- client/tailwind.config.js (verify accent, hardRed shadow tokens exist)
</read_first>
<acceptance_criteria>
- client/src/components/ui/Card.jsx exists
- Exports Card as named export
- Accepts decoration prop ('tape'|'tack'|'none', default 'none')
- Accepts rotate prop (number, default 0)
- Accepts children and className props
- Has inline style borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Has inline style transform: `rotate(${rotate}deg)`
- Uses bg-white class
- Uses shadow-hard class
- Uses p-6 class
- Uses relative class
- tape decoration renders a div with bg-gray-400 and opacity-40
- tape decoration div has absolute positioning
- tape decoration div has left-1/2 and -translate-x-1/2 (centered horizontally)
- tack decoration renders a div with bg-accent
- tack decoration div has rounded-full (circle shape)
- tack decoration div has shadow-hardRed
- tack decoration div has absolute positioning
- tack decoration div has left-1/2 and -translate-x-1/2 (centered horizontally)
</acceptance_criteria>
</task>

<task id="04-02-03">
<name>Create TextArea component with wobbly border and blue focus</name>
<action>
Create `client/src/components/ui/TextArea.jsx` with wobbly border and blue focus state.

Component signature: `TextArea({ value, onChange, placeholder, disabled = false, className = '' })`

From CLAUDE.md TextArea Component section:
- Full wobbly border (inline borderRadius), no underline-only style
- Border: 2px solid pencil color (#2d2d2d) via inline style
- Font: font-body class (Patrick Hand)
- Text color: text-pencil
- Text size: text-lg
- Full width: w-full
- Padding: p-4
- No resize: resize-none
- Background: bg-white
- Focus state: border switches to blue (#2d5da1), ring-2 in blue/20
- Disabled state: opacity-50, cursor-not-allowed
- Transitions: transition-colors duration-100
- Min height for textarea: min-h-[150px]

```jsx
export function TextArea({ value, onChange, placeholder, disabled = false, className = '' }) {
  return (
    <textarea
      value={value}
      onChange={onChange}
      placeholder={placeholder}
      disabled={disabled}
      style={{
        borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px',
        borderColor: '#2d2d2d',
        borderWidth: '2px',
        borderStyle: 'solid',
      }}
      className={`
        font-body text-lg text-pencil
        w-full p-4 bg-white
        resize-none min-h-[150px]
        transition-colors duration-100
        focus:outline-none focus:border-blue focus:ring-2 focus:ring-blue focus:ring-opacity-20
        disabled:opacity-50 disabled:cursor-not-allowed
        ${className}
      `}
    />
  )
}
```
</action>
<read_first>
- CLAUDE.md (TextArea Component section)
- client/tailwind.config.js (verify blue color token, body font token exist)
</read_first>
<acceptance_criteria>
- client/src/components/ui/TextArea.jsx exists
- Exports TextArea as named export
- Accepts value, onChange, placeholder, disabled, className props
- Has inline style borderRadius: '255px 15px 225px 15px / 15px 225px 15px 255px'
- Has inline style borderColor: '#2d2d2d'
- Has inline style borderWidth: '2px'
- Has inline style borderStyle: 'solid'
- Uses font-body class
- Uses text-lg class
- Uses text-pencil class
- Uses w-full class
- Uses p-4 class
- Uses bg-white class
- Uses resize-none class
- Uses focus:outline-none class
- Uses focus:border-blue class
- Uses focus:ring-2 class
- Uses focus:ring-blue class
- Uses focus:ring-opacity-20 class
- Uses disabled:opacity-50 class
- Uses disabled:cursor-not-allowed class
</acceptance_criteria>
</task>

<task id="04-02-04">
<name>Create ScoreBar component with 5-circle rating</name>
<action>
Create `client/src/components/ui/ScoreBar.jsx` — visual 1–5 rating using 5 circles.

Component signature: `ScoreBar({ score })`

From CLAUDE.md Score Bar section:
- Renders exactly 5 circles
- Filled circles (num <= score): background #2d2d2d (pencil) with hard shadow '4px 4px 0px 0px #2d2d2d'
- Empty circles (num > score): background #e5e0d8 (muted), shadow: 'none'
- Each circle: 24px x 24px, borderRadius 50%
- Row has inline transform: rotate(-1deg)
- Layout: flex with gap-2 between circles
- Transitions: all 200ms ease-out

```jsx
export function ScoreBar({ score }) {
  return (
    <div
      style={{ transform: 'rotate(-1deg)' }}
      className="flex gap-2"
    >
      {[1, 2, 3, 4, 5].map((num) => {
        const filled = num <= score
        return (
          <div
            key={num}
            style={{
              width: '24px',
              height: '24px',
              borderRadius: '50%',
              backgroundColor: filled ? '#2d2d2d' : '#e5e0d8',
              boxShadow: filled ? '4px 4px 0px 0px #2d2d2d' : 'none',
              transition: 'all 200ms ease-out',
            }}
          />
        )
      })}
    </div>
  )
}
```
</action>
<read_first>
- CLAUDE.md (Score Bar section)
- client/tailwind.config.js (verify pencil and muted hex values match: #2d2d2d and #e5e0d8)
</read_first>
<acceptance_criteria>
- client/src/components/ui/ScoreBar.jsx exists
- Exports ScoreBar as named export
- Accepts score prop (number)
- Renders exactly 5 elements via map over [1, 2, 3, 4, 5]
- Filled circles (num <= score): inline backgroundColor: '#2d2d2d'
- Filled circles have inline boxShadow: '4px 4px 0px 0px #2d2d2d'
- Empty circles (num > score): inline backgroundColor: '#e5e0d8'
- Empty circles have inline boxShadow: 'none'
- Each circle: inline width: '24px' and height: '24px'
- Each circle: inline borderRadius: '50%'
- Each circle: inline transition: 'all 200ms ease-out'
- Container row has inline style transform: 'rotate(-1deg)'
- Container uses flex layout with gap-2
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. All 4 files should exist in client/src/components/ui/
2. Each component is independently importable
3. Visual checks (when rendered in app):
   - Buttons: wobbly organic curves, hard offset shadow, Patrick Hand font
   - Card: wobbly border, optional tape bar or tack circle decoration
   - TextArea: wobbly border, blue focus ring, no resize handle in corner
   - ScoreBar: 5 circles, filled ones dark with hard shadow, row tilted -1deg
4. Color: ALL text uses text-pencil (#2d2d2d), no pure black (#000000)

All four tasks are independent — can execute in parallel or any order.
</verification>
